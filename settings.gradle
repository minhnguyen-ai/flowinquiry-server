
pluginManagement {
    repositories {
    }
    plugins {
        id 'com.gorylenko.gradle-git-properties' version "${gitPropertiesPluginVersion}"
    }
}

rootProject.name = "flowinquiry"
include 'apps:backend:server'
include 'apps:backend:tools:liquibase'
include 'apps:backend:commons'


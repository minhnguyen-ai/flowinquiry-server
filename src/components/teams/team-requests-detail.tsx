import { ViewProps } from "@/components/ui/ext-form";
import { TeamRequestType } from "@/types/teams";

const TeamRequestDetailView = ({
  entity: teamRequest,
}: ViewProps<TeamRequestType>) => {
  return <div>Team Request</div>;
};

export default TeamRequestDetailView;

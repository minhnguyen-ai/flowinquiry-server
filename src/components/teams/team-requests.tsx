"use client";

import { ViewProps } from "@/components/ui/ext-form";
import { TeamType } from "@/types/teams";

const TeamRequestsView = ({ entity: team }: ViewProps<TeamType>) => {
  return <div>Team Requests</div>;
};

export default TeamRequestsView;

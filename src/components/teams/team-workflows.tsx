"use client";

import { ViewProps } from "@/components/ui/ext-form";
import { TeamType } from "@/types/teams";

const TeamWorkflowsView = ({ entity: team }: ViewProps<TeamType>) => {
  return <div>Team Workflow</div>;
};

export default TeamWorkflowsView;
